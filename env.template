# Kia Metadata Generator - env.template
# 사용 방법:
# - 이 파일을 복사해서 ".env" 파일을 만드세요.
#   Windows: copy env.template .env
#   macOS/Linux: cp env.template .env
#
# 주의:
# - ".env"는 민감정보를 포함할 수 있으니 절대 Git에 커밋/공유하지 마세요.

# ===== GCP 설정 (필수) =====
GCP_PROJECT_ID=your-gcp-project-id
GCP_CREDENTIALS_PATH=./credentials/gcp-service-account.json

# ===== Document AI 설정 (필수) =====
DOCUMENTAI_PROCESSOR_ID=your-processor-id
DOCUMENTAI_LOCATION=us
# 동기(process_document) 처리 크기 한도(바이트). 기본 20MB.
# 큰 파일은 스킵되므로 필요하면 조정하세요(권장: 기본 유지).
DOCUMENTAI_MAX_SYNC_BYTES=20971520

# ===== 입력 설정 (둘 중 하나만 설정) =====
# 단일 파일 모드: python main.py
GCS_INPUT_PATH=gs://your-bucket/input/your_file.pptx

# 배치 모드: python main.py --batch
# 1) 버킷 전체(권장): 버킷에 업로드된 모든 파일을 처리
GCS_INPUT_BUCKET=input-data
# (선택) 특정 prefix 아래만 처리하고 싶으면 설정 (예: input/)
GCS_INPUT_PREFIX=
#
# 2) 레거시(폴더 URI): 특정 폴더만 처리 (기존 방식, 필요 시만 사용)
# GCS_INPUT_FOLDER=gs://input-data/input/

# ⚠️ 주의: "input-data"가 버킷이 아니라 폴더(prefix)인 경우
# 예) 버킷이 kia-demo이고 폴더가 input-data/ 라면 아래처럼 설정하세요:
# GCS_INPUT_BUCKET=kia-demo
# GCS_INPUT_PREFIX=input-data/

# ===== 출력 설정 (필수) =====
GCS_OUTPUT_BUCKET=your-bucket
GCS_OUTPUT_PATH=output/metadata/

