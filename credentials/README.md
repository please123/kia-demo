# credentials 폴더 안내

이 폴더에는 **GCP 서비스 계정 키(JSON)** 를 넣습니다.

- 파일명 권장: `gcp-service-account.json`
- `.env`의 `GCP_CREDENTIALS_PATH`는 기본으로 `./credentials/gcp-service-account.json`을 가리키도록 되어 있습니다.

주의: 이 폴더/키 파일은 절대 외부에 공유하거나 Git에 커밋하지 마세요.

